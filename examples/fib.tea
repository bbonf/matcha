import sys

def fibonacci(n):
    if n < 2:
        return 1

    return fibonacci(n-1) + fibonacci(n-2)

def say_hello(name):
    return "hello " + name

sys.log(1,2,3)
sys.log(fibonacci(5))
sys.log(say_hello("world"))

